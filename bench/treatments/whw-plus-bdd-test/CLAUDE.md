# Taskboard

A CLI task management tool built with Python.

## Test Command

Use the `bdd_test` MCP tool to run tests instead of running pytest directly. `bdd_test` runs the full test suite and reports results.

## Build & Test
- Run tests: use `bdd_test()` MCP tool
- Run app: `python -m taskboard <command>`
