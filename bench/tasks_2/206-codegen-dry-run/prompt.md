Add dry-run validation to code generation. Add a `validate_generated_code(project: Project) -> dict` function to `code_generator.py` that generates code and validates it by compiling (using `compile()`) without executing it. Return `{"valid": bool, "errors": list[str], "warnings": list[str]}`. Warnings should include things like: agents with no instruction, tools referencing non-existent agents, missing model configurations. Add a `POST /api/projects/{project_id}/validate` endpoint.
