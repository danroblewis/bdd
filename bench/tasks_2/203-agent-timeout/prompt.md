Add execution timeout support to RuntimeManager. Add a `timeout_seconds` parameter (default: None) to `RuntimeManager.run_agent()`. When set, the agent execution should be cancelled after the specified number of seconds, yielding a RunEvent with type "error" and a message like "Execution timed out after N seconds". The timeout should wrap the main `async for event in runner.run_async(...)` loop.
