Add a batch model connectivity test endpoint. Add a `POST /api/models/test-connectivity` endpoint in `main.py` that accepts `{"models": [{"provider": "gemini", "model_name": "gemini-2.0-flash"}, ...]}` and returns connectivity test results for each model. The actual testing logic should go in `model_service.py` as `async def test_model_connectivity(provider: str, model_name: str) -> dict` which returns `{"reachable": bool, "error": str|null, "latency_ms": float|null}`. Use a short timeout (5s) for each test.
